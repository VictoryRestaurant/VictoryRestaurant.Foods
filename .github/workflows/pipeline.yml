name: Continious-Integration

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Setup .NET
      uses: actions/setup-dotnet@v3 
      with:
        dotnet-version: 7.0.x
    - name: Build
      run: | 
        dotnet restore ./VictoryRestaurant.Foods.sln 
        dotnet build src/VictoryRestaurant.Foods.Presentation/VictoryRestaurant.Foods.Presentation.csproj --configuration Release --no-restore
    - name: Test
      run: dotnet test ./VictoryRestaurant.Foods.sln --no-build --verbosity normal

#on:
#  push:
#    branches:
#    - main

#jobs:
#  test:
#    runs-on: ubuntu-latest

#    steps:
#    - name: Install .NET
#      uses: actions/setup-dotnet@v3
#    with:
#        dotnet-version: 7.0.x
#    - name: Restore dependencies
#      run: dotnet restore
#    - name: Build
#      run: dotnet build --no-restore
#    - name: Test
#      run: dotnet test --no-build --verbosity normal